/* Here are the prototypes for the two functions to be written.  Write these
 * functions in the file functions.c (there are skeleton versions of the
 * functions there; just fill in their code), which has descriptions of what
 * the functions are supposed to do.
 *
 * The main functions in the files public1.c, public2.c, and public3.c have
 * some sample calls to test the functions on some values that are entered
 * when the program is run, but these don't test all cases for which the
 * functions should work.  Write additional tests of your own as well.
 */

int num_unique_values(int w, int x, int y);
int leftmost_digit(int num);
